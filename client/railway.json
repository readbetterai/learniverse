{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "client/Dockerfile",
    "watchPatterns": [
      "client/**",
      "types/**",
      "package.json"
    ]
  }
}
